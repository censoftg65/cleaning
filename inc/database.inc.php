<?php

error_reporting(0);

define (DB_HOST, "localhost");

define (DB_SERVER, "mysql");

define (DB_USER, "root");

define (DB_PASSWORD, "password");

define (DB_DATABASE, "centurys_db_cleaning");

?>